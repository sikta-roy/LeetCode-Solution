class Solution {
    public int[] twoSum(int[] nums, int target) {
        Map<Integer,Integer> d=new HashMap<>();
        int x,y;
        for(int i=0;;i++)
        {
            x=nums[i];
            y=target-nums[i];
            if(d.containsKey(y))
            {
                return new int[] {d.get(y),i};
            }
            d.put(x,i);
            
        }
        
    }
}
